const initialState = {}
/**
 * Right now there is no use of this reducer. But created for the sake of preserving consistency.
 * @param state
 * @param action
 * @returns {{}}
 */
const watchLocationSearchInputReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default watchLocationSearchInputReducer;